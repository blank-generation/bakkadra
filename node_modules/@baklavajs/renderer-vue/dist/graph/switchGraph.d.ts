import { Ref } from 'vue';
import { Editor, Graph, GraphTemplate } from '@baklavajs/core';
export type SwitchGraph = (newGraph: Graph | GraphTemplate) => void;
export declare function useSwitchGraph(editor: Ref<Editor>, displayedGraph: Ref<Graph>): {
    switchGraph: SwitchGraph;
};
