import { AbstractNode } from '@baklavajs/core';
export declare function getNodePosition(node: AbstractNode): {
    x: number;
    y: number;
};
export declare function setNodePosition(node: AbstractNode, x: number, y: number): void;
