import { Ref } from 'vue';
import { IMenuItem } from '../contextmenu';
import { IBaklavaViewModel } from '../viewModel';
export declare function useContextMenu(viewModel: Ref<IBaklavaViewModel>): {
    show: Ref<boolean, boolean>;
    x: Ref<number, number>;
    y: Ref<number, number>;
    items: import('vue').ComputedRef<IMenuItem[]>;
    open: (ev: MouseEvent) => void;
    onClick: (value: string) => void;
};
