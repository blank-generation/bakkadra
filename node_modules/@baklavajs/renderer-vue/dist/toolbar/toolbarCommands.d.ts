import { Component } from 'vue';
export interface ToolbarCommand {
    command: string;
    title: string;
    icon?: Component;
}
export declare const TOOLBAR_COMMANDS: Record<string, ToolbarCommand>;
export declare const DEFAULT_TOOLBAR_COMMANDS: ToolbarCommand[];
export declare const TOOLBAR_SUBGRAPH_COMMANDS: Record<string, ToolbarCommand>;
export declare const DEFAULT_TOOLBAR_SUBGRAPH_COMMANDS: ToolbarCommand[];
