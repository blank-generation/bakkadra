import { Editor, CalculationResult } from "@baklavajs/core";
/**
 * Apply the calculation result values to the output interfaces in the graph
 * @param result Calculation result
 * @param editor Editor instance
 */
export declare function applyResult(result: CalculationResult, editor: Editor): void;
